# TinyFeed — TinyPhone 📱

โทรศัพท์จำลองสำหรับโรลเพลย์ใน **SillyTavern** — มีหน้าโฮมและแอปในตัว **10 แอป** ให้ตัวละคร/NPC มีชีวิตนอกบทสนทนาหลัก เสริมบรรยากาศ worldbuilding และ (ถ้าเปิด) ทักหาเราเองได้แม้ไม่ได้เปิดแอปอยู่

> เปิดจากเมนู extensions (ไอคอนไม้กายสิทธิ์ข้างกล่องพิมพ์) → เข้าหน้าโฮม → เลือกแอป

---

## 📲 แอปในเครื่อง

### 📱 TinyFeed — ฟีดโซเชียล + ข่าวสาร
- โพสต์เอง หรือกดให้ AI โพสต์ในนามตัวละครหลัก/NPC (เลือกผู้โพสต์โดยกดรูปโปรไฟล์ในแถบเขียน) · ไลค์ · แชร์ · คอมเมนต์ · หน้ารายละเอียดโพสต์
- **ปุ่ม "ให้ NPC คอมเมนต์"** · **ลบคอมเมนต์** · **AI ตอบคอมเมนต์** (ตอบทันที / กดปุ่มขอ) · **คอมเมนต์ NPC ติดโพสต์ใหม่** (ไม่มี / AI เลือกจำนวน / กำหนดเอง)
- **สร้างโพสต์อัตโนมัติ** ทุก N ข้อความ / ให้ AI ตัดสินใจ / **เมื่อเจอคีย์เวิร์ด** · **แท็บข่าวสาร** AI แต่งข่าวเสริมโลก
- **ส่งสติกเกอร์/รูปจาก TinyGallery** ในโพสต์+คอมเมนต์ได้ (โทเคน `[sticker:]`/`[img:]`) · **เพ็ทก็โพสต์ลงฟีดเองได้** (จาก TinyPet)

### 💬 TinyConnect — แชตสไตล์ไลน์
- แชตเดี่ยวกับตัวละครหลัก/NPC · **แชตกลุ่ม** (ตั้งชื่อ + สมาชิก + ตั้งรูปกลุ่มได้) · **แชตกับเพ็ท** (เพ็ททักเราเองตอนหิว/เหงา)
- **แถบพิมพ์สไตล์ไลน์:** `(+เมนูเสริม) (รูป) (ช่องพิมพ์+สติกเกอร์ในช่อง) (ส่ง)` · ปุ่ม **(+)** = เมนูฟังก์ชันเสริม (ตอนนี้มีโอนเงิน, รองรับเพิ่มในอนาคต)
- **สติกเกอร์/รูปแยกบับเบิลเสมอ** (แม้อยู่กลางข้อความ) · แยกหลายบรรทัดเป็นหลายบับเบิล (เปิด/ปิดได้) · ลบบับเบิลได้
- **โอนเงิน (สลิป)** ระหว่างเรา↔คู่แชท (หัก/เข้า TinyBank) · **กลุ่มคุยกันเอง** · **คู่แชททักหาเราเองอัตโนมัติ** (ทุก N ข้อความ / AI / คีย์เวิร์ด)

### 🎥 TinyStream — ไลฟ์สตรีม + คอมเมนต์สด
- เริ่ม/จบไลฟ์ (AI ตั้งหัวข้อ + สุ่มผู้ชม) · **เลือกสตรีมเมอร์ในหน้าแอป** (ตัวละครหลัก/NPC/เราเอง) · **ไลฟ์หลายคน** (co-host — แตะรูปให้พูด/วนคิว) · **ลบไลฟ์เก่า** ได้
- คอมเมนต์สดผู้ชม AI 3 โหมด (กดโหลด / อัตโนมัติ / ตอนเราคอมเมนต์) · พิมพ์คอมเมนต์เองได้ · **สตรีมเมอร์หลายคนพูดได้ในเจนเดียว**
- **โดเนท SuperChat** — AI ให้ผู้ชมโดเนท (เข้า TinyBank ถ้าเราเป็นคนไลฟ์) · **เราโดเนทให้สตรีมเมอร์คนอื่นได้** (ไอคอนในแถบคอมเมนต์)

### 📅 TinyMemo — กำหนดการ + โน้ต/ความจำ
- **แท็บกำหนดการ** — ไทม์ไลน์อีเวนต์ที่กำลังจะเกิด · ติ๊กเสร็จ / AI ขีดฆ่าอันที่ผ่านไป · **แท็บโน้ต/ความจำ** — บันทึกเหตุการณ์/ข้อเท็จจริงที่อยากให้จำ
- ปุ่ม **"สแกน"** ครั้งเดียวได้ทั้งสองแท็บ (auto/keyword ได้) · เพิ่มเอง/ลบได้ · ใช้เป็น **แหล่งความจำให้ตัวละคร RP** ได้

### 🗣️ TinyForum — เว็บบอร์ด/กระทู้
- ตั้งกระทู้เอง หรือ "ให้ชาวเน็ตตั้งกระทู้" (AI สร้างจากเนื้อเรื่อง) · แท็บ **ล่าสุด / มาแรง** · คอมเมนต์เยอะ + **ตอบกลับซ้อน 1 ชั้น** + **โหลดคอมเมนต์เพิ่ม**
- ไลค์/ลบ ได้ · **ระบบห้อง/หมวด** เพิ่ม-แก้เองในตั้งค่า (AI เลือกจากรายการ) · auto/keyword ได้

### 🖼️ TinyGallery — คลังรูป + สติกเกอร์ *(global)*
- เก็บ **รูป + สติกเกอร์** เป็นอัลบั้ม (ข้ามแชท) · ส่งด้วยโทเคน `[img:ชื่อ]` / `[sticker:ชื่อ]` — แสดงผลอัตโนมัติในทุกแอป (feed/connect/stream/forum)
- **รูป > สติกเกอร์** — ถ้าส่งพร้อมกัน แสดงแค่รูป · ตั้งได้ว่าให้ AI เข้าถึงอัลบั้มไหน (ส่งสติกเกอร์/รูปเองได้)

### 💰 TinyBank — ธนาคาร/การเงิน *(ผูกกับแชท)*
- ยอดเงิน + ประวัติธุรกรรม (แยกตามเนื้อเรื่อง) · เพิ่ม/จ่ายเงินเอง · เป็นแหล่งเงินกลางให้ทุกแอป (สลิป TinyConnect · โดเนท TinyStream · ซื้อของ TinyShop · เติมเหรียญ TinyPet)

### 🛍️ TinyShop — ร้านค้า *(แคตตาล็อก global · ซื้อผูกกับแชท)*
- เพิ่มสินค้าเอง (ชื่อ/ราคา/**รูปหรืออิโมจิ**/หมวด/รายละเอียด) หรือ **"ให้ AI สร้างสินค้า"** เข้ากับโลกของเรื่อง · **ปุ่มแก้ไข** ต่อสินค้า
- **หมวดหมู่** เพิ่ม-ลบเองในตั้งค่า + แถบกรองหมวด · ซื้อด้วยเงิน TinyBank

### 🐾 TinyPet — สัตว์เลี้ยงเสมือน (ทามาก็อตจิ) *(global)*
- เลี้ยงสัตว์ 1 ตัว **ตัวเดียวกันทุกแชท** · ค่า **อิ่ม/พลังงาน/สะอาด/อารมณ์/สุขภาพ** ลดตาม **เวลาจริง** แม้ปิด ST (มีเพดานกันหายนานแล้วตายทันที)
- **ให้อาหาร/เล่น/อาบน้ำ/นอน** (cooldown + ให้เยอะไปมีผลเสีย) · **sprite ตามสถานะ** (ใส่ลิงก์/วางไฟล์เอง `assets/pet-sprites/<สถานะ>.png` · ไม่มี = อิโมจิ)
- **ความผูกพัน (bonding)** เพิ่มจากการดูแล + **เอ่ยชื่อเพ็ทในบทบาท RP** → ยิ่งผูกพันเพ็ทยิ่งอารมณ์ดีคงทน
- **วิวัฒนาการ (evolution)** — โตตามความผูกพัน: **เด็ก → วัยรุ่น (bond Lv.3) → โตเต็มวัย (bond Lv.6)** · ตอนโตมีแจ้งเตือน + เพ็ททักมาฉลอง · ใส่ sprite ต่อระยะได้ (`<ระยะ>_<สถานะ>.png` เช่น `teen_idle.png` · ว่างไว้ = ใช้ร่างเด็ก)
- **ร้านสัตว์เลี้ยง** แยกต่างหาก — ใช้ **"เหรียญเพ็ท"** (เติมจาก TinyBank / รับจากมินิเกม) ซื้ออาหาร/ของเล่น/ของใช้ (มี effect ต่างกัน) · AI สร้างไอเทม/แก้ไขได้
- **มินิเกม** — จับคู่การ์ด · หา emoji ที่ต่าง (แตะตัวที่ต่าง กริดโตขึ้นเรื่อยๆ, จับเวลา 40 วิ) · ตีตัวตุ่น (ตี 🐹 เลี่ยง 💣, จับเวลา 35 วิ) — เล่นแล้วได้เหรียญ+อารมณ์+ผูกพัน, ใช้พลังงาน
- **AI แต่งบทพูดเพ็ท** ตามสถานะ + คำสั่งเสริม (นิสัย/สายพันธุ์) · เพ็ท **โพสต์ลงฟีด / ทักในแชท** เองได้ · ตายจริง → **รับเลี้ยงตัวใหม่**

### 🌐 TinyVerse — ฮับตัวละครรวม (crossover) *(global)*
- **แท็บ ฟีด / ตัวละคร** — ฟีดโกลบอลรวมโพสต์จากตัวละครทุกการ์ดไว้ที่เดียว
- **Import แบบเลือกได้** — เลือกเฉพาะการ์ด+NPC ที่ต้องการจาก SillyTavern (ดึงชื่อ/รูป/คำอธิบาย) · ตั้งค่าจำกัดตัวอักษร bio · ปุ่มเคลียร์ทั้งหมด
- **ช่องเขียนแบบ TinyFeed** — แตะรูปโปรไฟล์เลือกคนโพสต์ (**ตัวเราเอง / สุ่ม / ตัวละคร**); โพสต์เองได้พร้อม **ปุ่มแนบรูป 🖼️ + สติกเกอร์ 😊** จากคลัง; เลือกตัวละคร = AI แต่งในน้ำเสียงตัวนั้นจาก persona · กดใจ/ลบได้
- **⭐ คอมเมนต์ข้ามการ์ด** — กด "ให้ตัวละครคอมเมนต์" แล้วตัวละครจาก **การ์ดอื่น** จะมาคอมเมนต์โพสต์นั้นในคาแรกเตอร์ของตัวเอง (คุณคอมเมนต์เองก็ได้)
- **🔮 ซีนครอสโอเวอร์** — แท็บ **ซีน**: เลือกตัวละคร 2–4 ตัวจากต่างการ์ด + ใส่ฉาก → AI แต่งบทสนทนาให้เจอกัน **โดยไม่ต้องเปิดแชทจริงใน ST** · บันทึก/เปิดดู/ลบซีนได้
- **หน้าโปรไฟล์ตัวละคร** (avatar/bio/NPC/persona แก้ได้) — ใช้ร่วมกับ TinyFeed ด้วย (**แตะชื่อผู้โพสต์ → เปิดโปรไฟล์**)

---

## 📢 ทักเชิงรุก + แจ้งเตือน *(ใหม่)*

### ตัวละครทักหาเราเอง (Proactive)
ตัวจับเวลาเบื้องหลังจะให้ตัวละครหลัก/NPC (สุ่มจากแชท ST ปัจจุบัน) **ทักเข้าห้องแชต TinyConnect เอง** เป็นระยะ — คุมด้วย:
- เว้นระยะขั้นต่ำ (นาที) + โอกาสต่อรอบ (%) + **ช่วงเงียบ (quiet hours)** ไม่ทัก
- สไตล์เสริม (ผสมได้): **ทักตอนเงียบนาน (idle)** · **ตามช่วงเวลาจริง** (เช้า/บ่าย/ดึก) · **ทักผ่านโพสต์ฟีดแทน DM**
- **กลุ่มคุยกันเองอัตโนมัติ** เป็นระยะ

> ⚠️ ทำงานเฉพาะตอน **เปิดแท็บ SillyTavern ค้างไว้** (ย่อ/พับได้ แต่ห้ามปิดแท็บ/เครื่อง sleep) และมีแชตที่มีตัวละครเปิดอยู่ · ไม่ต้องเปิดห้องแชตของใครค้างไว้ ระบบสุ่มเลือกให้เอง · ยิง AI เป็นระยะ = เปลือง token

### แจ้งเตือน 3 ชั้น
- **แบนเนอร์** เลื่อนลงบนจอ (กดแล้วเข้าห้อง/แอปที่เกี่ยว) + จุดแดงนับเลขบนปุ่มเมนู + สั่น
- **ลิ้นชักในโทรศัพท์** — ไอคอน 🔔 บน topbar เก็บประวัติ 30 รายการล่าสุด (แตะเข้าแอป, ล้างได้) · เก็บถาวรข้าม reload
- **แจ้งเตือน OS/desktop จริง** (เปิด/ปิดได้, default ปิด) — ยิงผ่าน Service Worker รองรับมือถือ · มีปุ่ม **"ทดสอบแจ้งเตือน OS"** บอกสาเหตุถ้าติด
  - ต้องเปิด ST ผ่าน **HTTPS หรือ http://localhost** (เปิดผ่าน IP/HTTP ธรรมดา เบราว์เซอร์จะบล็อก)
  - ⚠️ ข้อความจะโผล่บน lock screen/แถบ OS แบบเห็นได้ — **ระวังเนื้อหา 18+**

### จำหน้าจอล่าสุด
เปิดแอปใหม่/หลัง reload จะกลับไปหน้า/ห้องเดิม (ไม่เด้งกลับโฮมทุกครั้ง)

---

## 🔗 การเชื่อมกับโรลเพลย์

- **แทรกเข้าประวัติแชทหลัก** (`setExtensionPrompt` ตำแหน่ง IN_CHAT, ตั้ง depth ได้) — เลือกได้ 5 แบบ: ไม่แทรก / เฉพาะโพสต์ / เฉพาะข่าว / โพสต์+คอมเมนต์ / โพสต์+คอมเมนต์+ข่าว · เพิ่ม checkbox **แทรกแชต TinyConnect / ไลฟ์ TinyStream / กำหนดการ+โน้ต TinyMemo / กระทู้ TinyForum** → ตัวละครอ้างอิงได้ เช่น *"เห็นที่โพสต์แล้วนะ" / "อย่าลืมนัดพรุ่งนี้"*
- **เชื่อมเนื้อหาข้ามแอป** — เป็น **checkbox รายแหล่ง** เลือกได้ว่าให้แอปอื่นเห็นอะไรบ้าง (โพสต์ / คอมเมนต์ / ข่าว / แชต / ไลฟ์ / กำหนดการ / กระทู้) · แชต TinyConnect ปิดไว้ default เพื่อความเป็นส่วนตัว
- **จำนวนล่าสุดที่แทรก** = นับ "ต่อแอป" (กี่โพสต์/ข่าว/ข้อความต่อคน/คอมเมนต์) · **depth** = ตำแหน่งความลึกในประวัติแชท ไม่ใช่จำนวน

## 🤖 ระบบ Generation

- ใช้ `generateQuietPrompt()` — สร้างเนื้อหาโดยไม่ปนบทสนทนาหลัก · **เลือกโมเดล/API แยกได้** ผ่าน Connection Manager (มี fallback)
- โหมดโมเดลแยกแนบบริบทให้เอง: ตัวละครหลัก · Persona · World Info (ครบทุกตำแหน่ง) · บทสนทนาล่าสุด
- **ตัดส่วน reasoning/thinking อัตโนมัติ** · ตั้ง token + คำสั่งเสริมได้แยกทุกแอป

---

## 🎨 ปรับแต่งหน้าตา *(ใหม่)*

- **ธีมมืด/สว่าง** + **สี accent** เลือกจาก preset 6 สี **หรือ color picker เอง** (คืนค่าเริ่มต้นได้)
- **ความทึบฟิลเตอร์พื้นหลัง** ปรับได้ (สไลเดอร์) · **สีไอคอนแอปตามธีม** · **พื้นหลังโฮมตามธีม + ปุ่มสุ่มสี** · **วอลเปเปอร์** ใส่ลิงก์รูปได้
- **วิดเจ็ตหน้าโฮม** — นาฬิกา+วันที่ (เดินจริง) · มินิกำหนดการ (จาก TinyMemo)
- **Custom CSS** — ใส่ CSS เองแต่งเพิ่มได้ (เช่น ติดสติกเกอร์ PNG บนบับเบิลแชต) · target ด้วย `.tinyfeed-*` / `#tinyfeed-*`
- Markdown เบาๆ (`**หนา**` `*เอียง*` `` `โค้ด` ``) + #แฮชแท็ก/@เมนชันสีธีม · รูปโปรไฟล์ override ได้ · เวลาแบบสัมพัทธ์ · รองรับจอเตี้ย/มือถือ (ใช้ `dvh`)

---

## ⚙️ หน้าตั้งค่า

เปิดด้วยไอคอนเฟืองใน topbar (ใช้ได้ทุกหน้า — ปิดแล้วกลับที่เดิม)

หมวดใหญ่ 6 กลุ่ม: **⚙️ ทั่วไป · 🎨 ธีม&CSS · 👤 ตัวละคร · 💰 การเงิน · 📱 เฉพาะแอป · 🔧 ขั้นสูง**

| กลุ่ม | ตั้งค่าอะไรได้ |
|---|---|
| **⚙️ ทั่วไป** | การแจ้งเตือน (+ OS notif + ปุ่มทดสอบ) · **ทักเชิงรุก** (เปิด/ระยะ/โอกาส/quiet hours/idle/time-aware/via-feed/กลุ่มคุยกันเอง) |
| **🎨 ธีม & CSS** | ธีม/สี accent/ฟิลเตอร์/ไอคอนตามธีม/พื้นหลังโฮม · วิดเจ็ต (นาฬิกา/มินิกำหนดการ/แดชบอร์ดโทเคน) · Custom CSS · วอลเปเปอร์ |
| **👤 ตัวละคร** | รูปโปรไฟล์+username/alias (ผู้ใช้/ตัวละคร) · NPC ประจำ |
| **💰 การเงิน** | TinyBank (สกุลเงิน/ตำแหน่งสัญลักษณ์) · โดเนท (เปิด/เพดาน/tier สี) · สลิป TinyConnect |
| **📱 เฉพาะแอป** | TinyFeed (ไลค์/history/auto/คอมเมนต์) · TinyStream · TinyConnect (+auto) · TinyMemo · TinyForum (+ห้อง) · **ทริกเกอร์คีย์เวิร์ด** (คำ+ขอบเขต+cooldown ต่อแอป) · **TinyGallery** (ให้ AI เข้าถึงอัลบั้ม) · **TinyShop** (หมวด/AI) · **TinyPet** (decay/sprite/AI/เหรียญ) |
| **🔧 ขั้นสูง** | โมเดล-API แยก · แทรกเข้า RP + เชื่อมข้ามแอป · **Prompt (แก้เอง)** |

---

## 🧩 การตั้งค่าขั้นสูง — แก้ Prompt เอง *(ใหม่)*

เผื่อ AI ไม่ทำตามรูปแบบ (เช่นได้คำบรรยายแทนโพสต์) แก้ prompt ของตัวสร้างเนื้อหาได้เอง — ครอบคลุม: โพสต์ฟีด · คอมเมนต์ติดโพสต์ · AI ตอบคอมเมนต์ · ข่าว · ตั้งกระทู้ · คอมเมนต์กระทู้ · สตรีมเมอร์พูด · บทพูดเพ็ท · โพสต์ของเพ็ท · สร้างสินค้า TinyShop · สร้างไอเทมร้านเพ็ท

**กติกา:**
- ต้องคง **marker** ที่ระบุไว้เสมอ (เช่น `POST:` / `COMMENT:` / `TITLE:`) — เพราะระบบใช้ marker นี้แยกผลลัพธ์ · ถ้าลบออก → ขอบแดง + ไม่บันทึก
- กด **"คืนค่าเริ่มต้น"** เพื่อรีเซ็ตเทมเพลตนั้น
- `{{...}}` = ช่องที่ระบบเติมข้อมูลให้อัตโนมัติตอนสร้าง (อย่าพิมพ์ทับ ให้วางไว้ในตำแหน่งที่ต้องการ)

### {{ }} แต่ละอันแทนอะไร

| Token | แทนที่ด้วย |
|---|---|
| `{{roster}}` | รายชื่อผู้โพสต์/คอมเมนต์ที่ให้ AI เลือก (ตัวละครหลัก + NPC ประจำ) |
| `{{extra}}` | คำสั่งเสริมที่ผู้ใช้พิมพ์ในช่อง "คำสั่งเสริม" ของแอปนั้น |
| `{{history}}` | โพสต์/หัวข้อข่าวล่าสุดที่มีอยู่แล้ว (กันเขียนซ้ำ) |
| `{{context}}` | เนื้อหาจากแอปอื่น (เมื่อเปิด "เชื่อมเนื้อหาข้ามแอป") |
| `{{postText}}` | ข้อความของโพสต์ที่กำลังคอมเมนต์ |
| `{{author}}` | ชื่อเจ้าของโพสต์ |
| `{{count}}` | คำสั่งจำนวนคอมเมนต์ที่ให้เขียน (ตามโหมดคอมเมนต์ติดโพสต์) |
| `{{thread}}` | คอมเมนต์ล่าสุดในโพสต์นั้น (ให้ AI ตอบต่อได้ตรงบริบท) |
| `{{rooms}}` | รายชื่อห้อง TinyForum ที่ให้ AI เลือก |
| `{{room}}` `{{title}}` `{{body}}` | ห้อง / หัวข้อ / เนื้อหา ของกระทู้ที่กำลังโหลดคอมเมนต์ |
| `{{batch}}` | จำนวนคอมเมนต์ต่อการโหลด 1 ครั้ง |
| `{{existing}}` | คอมเมนต์+รีพลายที่มีอยู่แล้วในกระทู้ (มีเลขกำกับ กันซ้ำ + ให้ตอบกลับได้) |

> แต่ละเทมเพลตหน้าตั้งค่าจะบอกไว้ว่าใช้ marker อะไร และมี token ไหนใช้ได้บ้าง

---

## 💾 การเก็บข้อมูล

- **ผูกกับแชทแต่ละอัน** (`chat_metadata.tinyfeed`) — ฟีด · ข่าว · แชต/กลุ่ม TinyConnect · ไลฟ์ TinyStream · กำหนดการ+โน้ต TinyMemo · กระทู้ TinyForum · **ยอดเงิน+ธุรกรรม TinyBank** · **ของที่ซื้อใน TinyShop** · หน้าจอล่าสุด → สลับแชทไม่ปนกัน
- **ส่วนกลาง ข้ามแชท** (`extension_settings.tinyfeed`) — ธีม/สี/วิดเจ็ต/Custom CSS · แจ้งเตือน+ทักเชิงรุก · **โปรไฟล์+NPC (ผูก persona/ตัวละคร)** · **คลัง TinyGallery** · **แคตตาล็อก TinyShop + หมวด** · **สัตว์เลี้ยง TinyPet ทั้งตัว (สถานะ/เหรียญ/ผูกพัน/ร้าน/แชตเพ็ท)** · ห้อง TinyForum · คีย์เวิร์ด · prompt ที่แก้เอง · config ทุกแอป
  - ⚠️ **TinyPet + TinyGallery + TinyShop-แคตตาล็อก เป็น global** (ตัวเดียวทุกแชท ไม่รีเซ็ตตอนสลับ RP)

---

## 📥 การติดตั้ง

1. **Extensions → Install Extension** วาง GitHub URL → Refresh (Ctrl+F5)
2. กดไอคอนไม้กายสิทธิ์ → เลือก **TinyFeed**

หรือวางไฟล์เองที่ `SillyTavern/public/scripts/extensions/third-party/tinyfeed/`

> ⚠️ ชื่อโฟลเดอร์ต้องเป็น `tinyfeed` เท่านั้น · แจ้งเตือน OS ต้องเปิด ST ผ่าน HTTPS/localhost

**ต้องมี:** SillyTavern ที่มี `getContext()` พร้อม `generateQuietPrompt`, `setExtensionPrompt` · (ไม่บังคับ) Connection Manager สำหรับโมเดลแยก

---

## 📁 โครงสร้างไฟล์

```
tinyfeed/
├── manifest.json    ข้อมูล extension
├── index.js         ตรรกะทั้งหมด (แอป, generation, storage, navigation, proactive)
├── phone.html       โครง UI โทรศัพท์ + โฮม + ทุกแอป + หน้าตั้งค่า
├── style.css        ธีม สี แอนิเมชัน (ตัวแปร --tf-*)
├── sw.js            service worker (แจ้งเตือน OS บนมือถือ)
├── settings.html    drawer ในแผง Extensions
└── README.md
```

## 🛠️ หมายเหตุสำหรับการพัฒนาต่อ

- ตัวแปรธีม `--tf-*` บน `#tinyfeed-phone` (dark/light) · `.tinyfeed-hidden` = `display:none !important` **โดยตั้งใจ**
- **เพิ่มแอปใหม่:** push เข้า `HOME_APPS` array (โฮมเป็น JS-rendered + paginated ไม่ใช่ markup คงที่) + view `.tinyfeed-app` + เคสใน `openApp()` (whitelist + branch) + `handleBack` · แท็บของแอปต้องใช้ **คลาสเฉพาะ** (เช่น `.tinyfeed-memo-tabs`) ไม่ใช้ `.tinyfeed-tabs` (กัน settings ซ่อนโดน)
- **เพิ่มหมวดตั้งค่า:** `.tinyfeed-settings-group` + ใส่ชื่อลง `SETTINGS_LAYOUT`
- **เพิ่ม prompt ที่แก้ได้:** เพิ่มใน `PROMPT_DEFS` (label/marker/tokens/default) แล้วให้ generator เรียก `buildPrompt(id, vars)`
- **แจ้งเตือน:** ทุกช่องทางผ่าน `showNotif(...)` → banner + `notifLog` (ลิ้นชัก) + `fireOsNotif` (SW)

## 💌 Credits

- Vibe Coding: April & Claude

### 💖 Special Thanks

- [Universal Extension Creator Prompt](https://discord.com/channels/1100685673633153084/1434557148641104003) by Chai/Ultraviolenc
